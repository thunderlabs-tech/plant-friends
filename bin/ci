#!/usr/bin/env sh
set -e

ROOT=$(cd $(dirname "$0")/..; pwd)

echo "📦 Installing dependencies"
cd ${ROOT}
yarn install --frozen-lockfile

cd web-client
./bin/prettier-check
./bin/eslint
./bin/test
./bin/typecheck
./bin/build

echo "✅ Done!"
